// Shared by every typeahead/search field in the app (chat DM search, address autocomplete,
// posts search, users search) so the "type at least N characters" threshold stays consistent
// and only needs changing in one place.
export const MIN_QUERY_LENGTH = 3;
